# 🎁 Wishlist Mini App

Красивое и функциональное Telegram Mini App приложение для создания списков желаний и обмена ими с друзьями.

[![Build Status](https://img.shields.io/badge/build-passing-brightgreen)](https://github.com/IlgamAbiu/wishlins-miniapp)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

## 🌟 Основные возможности

- **Управление событиями**: Создавайте праздники, дни рождения или просто списки желаний.
- **Списки желаний**: Добавляйте подарки с указанием цены, ссылки на товар и приоритета.
- **Бронирование**: Друзья могут отмечать подарки как "забронированные", чтобы не купить одно и то же.
- **Интеграция с Telegram**: Автоматическая авторизация через Telegram WebApp и поддержка тем оформления.
- **Deep Linking**: Возможность делиться ссылкой на конкретное событие (через `startapp` параметр).
- **Полный стек**: Собственный Express сервер и легковесная база данных SQLite.

## 🛠 Технологический стек

- **Frontend**: React 19, Vite, Tailwind CSS (CDN), Lucide React.
- **Backend**: Node.js, Express 5.
- **База данных**: SQLite (через `better-sqlite3`).
- **Bot API**: Telegraf.
- **Деплой**: PM2, Nginx, Certbot (SSL).

## 🚀 Быстрый старт

### Требования
- Node.js 20+
- npm

### Установка
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/IlgamAbiu/wishlins-miniapp.git
   cd wishlins-miniapp
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Настройте переменные окружения:
   Создайте файл `.env` в корне проекта:
   ```env
   PORT=3010
   BOT_TOKEN=ваш_токен_от_BotFather
   WEB_APP_URL=https://your-domain.com
   ```

### Запуск
- **Разработка (Frontend)**: `npm run dev`
- **Запуск сервера**: `npm run server`
- **Сборка проекта**: `npm run build`

## ☁️ Деплой на сервер (VPS)

Проект настроен для работы с PM2.

1. Соберите фронтенд: `npm run build`
2. Запустите через PM2:
   ```bash
   pm2 start npm --name wishlist -- run server
   ```
3. Настройте Nginx как Reverse Proxy на порт `3010` и установите SSL через Certbot.

## 📁 Структура проекта

- `/server` — Express сервер, логика бота и работа с БД.
- `/components` — React компоненты интерфейса.
- `/services` — API клиенты и логика работы с данными.
- `/data` — Директория для локальной базы данных SQLite (игнорируется в Git).
- `/dist` — Собранный фронтенд для продакшена.

## 🤝 Контакты

Разработано как Telegram Mini App для удобного планирования подарков.

Если у вас есть вопросы или предложения — создавайте Issue или Pull Request!
