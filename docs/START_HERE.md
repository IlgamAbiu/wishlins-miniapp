# 🚀 Начало работы с проектом

Добро пожаловать в проект Wishlist Mini App!

## Быстрый старт (1 минута)

```bash
# 1. Запустите проект
make dev

# или
./dev.sh
```

Вот и всё! Проект запущен и готов к разработке.

## 📍 Что дальше?

### Доступ к сервисам

- **Frontend**: http://localhost:5173 - Vue 3 приложение с hot reload
- **Backend**: http://localhost:8000 - FastAPI с автоперезагрузкой
- **API Docs**: http://localhost:8000/docs - Swagger документация
- **PostgreSQL**: localhost:5432 - База данных

### Структура проекта

```
├── frontend/        # Vue 3 приложение
├── backend/         # FastAPI сервер
├── bot/             # Telegram бот
└── Documentation/   # Документация
```

### Основные команды

```bash
make dev             # Запустить все сервисы
make stop            # Остановить все сервисы
make logs            # Просмотр логов
make restart         # Перезапустить сервисы
make help            # Показать все команды
```

### Просмотр логов

```bash
# Все сервисы
make logs

# Конкретный сервис
make logs-frontend
make logs-backend
make logs-bot
```

### Работа с БД

```bash
make db-migrate      # Применить миграции
make db-shell        # Открыть psql консоль
make db-reset        # Сбросить БД (осторожно!)
```

## 📚 Документация

- **[DEVELOPMENT.md](DEVELOPMENT.md)** - Полное руководство по разработке
- **[CHEATSHEET.md](CHEATSHEET.md)** - Шпаргалка с командами
- **[README.md](README.md)** - Общее описание проекта
- **[WORKFLOW.md](WORKFLOW.md)** - Workflow после коммита

## 🔧 Разработка

### Изменение фронтенда

Все изменения в папке `frontend/src/` автоматически применяются (hot reload):

```bash
cd frontend/src/
# Редактируйте файлы, изменения применятся автоматически
```

### Изменение бэкенда

Все изменения в папке `backend/src/` автоматически применяются:

```bash
cd backend/src/
# Редактируйте файлы, сервер перезапустится автоматически
```

### Изменение бота

Изменения в папке `bot/src/` применятся после перезапуска:

```bash
# Внесите изменения, затем:
make restart-bot
```

## 🧪 Тестирование в Telegram

Для тестирования Mini App в реальном Telegram:

```bash
# 1. Установите ngrok
brew install ngrok

# 2. Запустите ngrok
ngrok http 5173

# 3. Скопируйте URL (например: https://abc123.ngrok-free.app)

# 4. Обновите .env
MINIAPP_URL=https://abc123.ngrok-free.app

# 5. Перезапустите бота
make restart-bot
```

## 🐛 Проблемы?

### Порт занят

```bash
lsof -i :5173  # Проверить порт
kill -9 <PID>  # Убить процесс
```

### Сервисы не запускаются

```bash
# Пересоздать всё
make stop
docker-compose down -v
make dev
```

### Изменения не применяются

```bash
# Перезапустить конкретный сервис
make restart-frontend
make restart-backend
```

## ❓ Нужна помощь?

1. Проверьте [DEVELOPMENT.md](DEVELOPMENT.md) - подробное руководство
2. Загляните в [CHEATSHEET.md](CHEATSHEET.md) - быстрая справка
3. Посмотрите логи: `make logs`
4. Проверьте статус: `make status`

---

**Удачной разработки! 🎉**

Начните с изучения кода:
- Frontend: [frontend/src/App.vue](frontend/src/App.vue)
- Backend: [backend/src/main.py](backend/src/main.py)
- Bot: [bot/src/main.py](bot/src/main.py)
